syntax = "proto3";

package identity.v1;

import "google/protobuf/timestamp.proto";

option go_package = "github.com/S7venKing/ticket-box-go/gen/identity/v1;identityv1";

// IdentityService owns user accounts.
// It is an internal service: authentication/authorization of end users is
// enforced by api-gateway, callers here are trusted services.
service IdentityService {
  rpc CreateUser(CreateUserRequest) returns (CreateUserResponse);

  rpc GetUserById(GetUserByIdRequest) returns (GetUserByIdResponse);

  rpc GetUserByEmail(GetUserByEmailRequest) returns (GetUserByEmailResponse);

  rpc UpdateUserProfile(UpdateUserProfileRequest) returns (UpdateUserProfileResponse);

  rpc ActivateUser(ActivateUserRequest) returns (ActivateUserResponse);

  rpc DeactivateUser(DeactivateUserRequest) returns (DeactivateUserResponse);
}

// =========================
// User
// =========================

// User is the public representation of an account.
// Credentials (password, password hash) are never part of this message.
message User {
  string id = 1;
  string email = 2;
  string full_name = 3;
  string phone = 4;
  bool is_active = 5;
  google.protobuf.Timestamp created_at = 6;
  google.protobuf.Timestamp updated_at = 7;
}

// =========================
// Create User
// =========================

message CreateUserRequest {
  string email = 1;
  // Plaintext password. Only ever hashed by the application layer; never logged.
  string password = 2;
  string full_name = 3;
  string phone = 4;
}

message CreateUserResponse {
  User user = 1;
}

// =========================
// Get User By ID
// =========================

message GetUserByIdRequest {
  string id = 1;
}

message GetUserByIdResponse {
  User user = 1;
}

// =========================
// Get User By Email
// =========================

message GetUserByEmailRequest {
  string email = 1;
}

message GetUserByEmailResponse {
  User user = 1;
}

// =========================
// Update User Profile
// =========================

message UpdateUserProfileRequest {
  string id = 1;
  string full_name = 2;
  string phone = 3;
}

message UpdateUserProfileResponse {
  User user = 1;
}

// =========================
// Activate User
// =========================

message ActivateUserRequest {
  string id = 1;
}

message ActivateUserResponse {
  User user = 1;
}

// =========================
// Deactivate User
// =========================

message DeactivateUserRequest {
  string id = 1;
}

message DeactivateUserResponse {
  User user = 1;
}
